name: ansible-test

on:
  push:

  pull_request:
    branches: ["main", "stable", "release/v*"]

jobs:

  build:
    name: Ansible Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Run ansible-lint
        uses: ansible/ansible-lint@main

  # ----- ----- ----- ----- -----

  test:
    needs:
      - build
    name: Ansible Molecule
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        config:
          - image: debian
            tag: bullseye

          - image: ubuntu
            tag: focal

          - image: fedora
            tag: "38"
    steps:
      - uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"

      - uses: robertdebock/molecule-action@5
        with:
          image: ${{ matrix.config.image }}
          tag: ${{ matrix.config.tag }}